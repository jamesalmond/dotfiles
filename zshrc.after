eval "$(rbenv init -)"

# ALIASES
alias be="bundle exec"
alias gst="git status"
alias dc="docker-compose -f docker-compose-dev.yml -f docker-compose.yml"
alias dce="dc exec app"
alias drake="dce bin/rake"
alias drails="dce bin/rails"
alias dspec="dce bin/rspec"
alias dbe="dce bundle exec"
alias drun="dc run app"
